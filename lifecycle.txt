# When you start working:
git pull origin master 
# pull = fetch changes + merge with local repo version


# regular lifecycle you can repeat:
git status
git add --all
# or
git add filedtoadd
git commit -m "comment"
git push origin master

#test
